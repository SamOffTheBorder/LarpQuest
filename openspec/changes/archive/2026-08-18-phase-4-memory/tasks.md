## 1. Database: vector storage, memory queue, arc summaries, context policy

- [x] 1.1 `supabase/migrations/20260819000001_chapter_memory.sql`: `create extension if not exists vector;`, `alter table chapters add column embedding vector(1536)`, `alter table chapters add column memory_status text not null default 'pending' check (memory_status in ('pending','complete','failed'))`, an `ivfflat` index on `chapters.embedding` (`vector_cosine_ops`), and an index on `memory_status` mirroring `chapters_extraction_idx`.
- [x] 1.2 Same migration: `memory_queue` table — `id`, `chapter_id references chapters on delete cascade`, `status text check (status in ('pending','claimed','complete','failed'))`, `attempt_count int default 0`, `last_error text`, `claimed_at timestamptz`, `created_at`, `updated_at`, `unique (chapter_id)`. Structurally mirrors `extraction_queue` (`20260812000005_extraction_queue.sql`). RLS: readable by `story_members` via `chapter_id -> chapters.story_id`; service-role-only write.
- [x] 1.3 Same migration: `claim_memory_job(stale_after interval)` RPC, copied in structure from `claim_extraction_job` — atomically claims the oldest `pending` row or a `claimed` row past `stale_after`.
- [x] 1.4 Same migration: `arc_summaries` table per build plan Part 8.2 — `id`, `story_id references stories on delete cascade`, `from_chapter int not null`, `to_chapter int not null`, `summary text not null`, `embedding vector(1536)`, `created_at`. `ivfflat` index on `embedding`. RLS gated through `story_members` via `story_id`, matching `chapters`.
- [x] 1.5 `supabase/migrations/20260819000002_context_policy.sql`: `alter table universe_versions add column context_policy jsonb not null default` the JSON-encoded default policy (`recent_chapters: 3, retrieved_chapters: 5, retrieval_bias: 'precedent', canon_compression: 'full', token_budget: 24000`); `alter table universe_versions add column canon_bible_summary jsonb`, `alter table universe_versions add column canon_bible_rules_only jsonb` (both nullable — populated by `publish_universe_version` going forward; existing rows keep them null and are only reachable by stories whose policy requests `full`, which needs neither). Also added `20260819000003_vector_extension_schema.sql` (not originally scoped): the advisor flagged `vector` installed in `public`; moved it to `extensions` schema, which `supabase/config.toml`'s `extra_search_path` already anticipated.
- [x] 1.6 `supabase db push` against the linked project, then `supabase db advisors --linked` and `supabase db query --linked --file supabase/tests/rls_coverage.sql` to confirm no RLS/search_path gaps, per CLAUDE.md. Advisors clean (only the pre-existing, unrelated leaked-password-protection warning remains); RLS coverage and append-only checks both passed (no exception raised).

## 2. Memory module: schemas, prompts, generation

- [x] 2.1 `apps/web/src/lib/memory/schemas.ts`: Zod `chapterSummarySchema` (what happened, who was involved, what changed — matching Part 6.1) and `contextPolicySchema` (`recent_chapters`, `retrieved_chapters`, `retrieval_bias` enum, `canon_compression` enum, `token_budget`), plus `DEFAULT_CONTEXT_POLICY` matching task 1.5's DB default exactly. Also added `arcSummaryResultSchema` (plain-text `summary`, not the structured chapter shape — an arc summary already compresses several structured summaries).
- [x] 2.2 `apps/web/src/lib/memory/prompts.ts`: `buildChapterSummaryPrompt(chapter, entities, bias)` for chapter-level summaries; `buildArcSummaryPrompt(chapterSummaries, bias)` for arc-level summaries; both accept the universe's `retrieval_bias` and append a short bias-specific instruction (precedent/information/emotional/thematic) per design.md decision 4 — no branching on universe or genre identity, only on the policy value passed in.
- [x] 2.3 `apps/web/src/lib/memory/generate.ts`: `generateChapterMemory(args)` — calls `callStructured` with `role: 'summarizer'` and the chapter summary schema; then calls the new `embedText` gateway function (added to `ai/gateway.ts` — the gateway previously had no embeddings support at all) with `role: 'embedder'` and the summary text. Both calls use an injected `UsageRecorder`. Returns a typed `MemoryOutcome` failure (mirrors `runStage`'s shape) rather than throwing. `persistChapterMemory` does the actual `chapters` row write, kept separate so `generateChapterMemory` stays testable without a DB mock.
- [x] 2.4 `apps/web/src/lib/memory/arc-compaction.ts`: constants `ARC_COMPACTION_THRESHOLD_CHAPTERS = 50`, `ARC_SIZE_CHAPTERS = 12`; `shouldCompactArc(currentTurnNumber): { fromChapter, toChapter } | null` — pure function, true exactly when `currentTurnNumber` closes an arc boundary past the threshold; `generateArcSummary(args)` — reads the covered chapters' summaries (not full prose), calls `summarizer` then `embedder`, writes an `arc_summaries` row.
- [x] 2.5 `apps/web/src/lib/memory/generate.test.ts`: chapter memory returns a complete outcome with summary + embedding on success and records usage for both calls; embeds the generated summary text, not the raw prose; malformed/throwing summarizer and a failing embedder each produce a typed failed outcome with correct usage recording. 6 tests, all passing.
- [x] 2.6 `apps/web/src/lib/memory/arc-compaction.test.ts`: `shouldCompactArc` boundary tests (below threshold, at threshold, first/second arc close, between boundaries); `generateArcSummary` writes a row keyed to the right chapter range built from chapter summaries (prompt content asserted), and a failed summarizer call writes no row. 6 tests, all passing. Full `npm run` typecheck across the module is clean.

## 3. Memory worker (post-publish hook)

- [x] 3.1 `apps/web/src/lib/engine/memory-worker.ts`: `runOneMemoryJob(staleAfterMinutes = 5)` — claims via `claim_memory_job`, calls `generateChapterMemory`, updates `memory_queue.status` and `chapters.memory_status`; after a successful chapter memory job, calls `shouldCompactArc` against the story's current chapter count and, if it returns a range, runs `generateArcSummary` inline (matching the worker's own synchronous style, per design.md decision 6). Resolves the story's pinned universe version's `retrieval_bias` for both calls, defaulting when unpinned. Structurally mirrors `extraction-worker.ts`'s split and failure-isolation comment block.
- [x] 3.2 Hooked `memory_queue` enqueue into `publish_chapter` via a new migration (`20260819000004_publish_chapter_memory_queue.sql`, `create or replace function`) rather than editing the original migration file, since already-applied migrations are immutable — added `insert into memory_queue (chapter_id, story_id) ... on conflict (chapter_id) do nothing` alongside the existing extraction enqueue, same transaction. Pushed to the linked project.
- [x] 3.3 `apps/web/src/app/api/worker/memory/route.ts`: route handler invoking `runOneMemoryJob`, mirrors `api/worker/extract/route.ts` exactly (shared-secret auth, always-200 response).
- [x] 3.4 `apps/web/src/lib/engine/memory-worker.test.ts`: 6 tests — generates and persists memory on success; never touches `extraction_status`/`extracted_diffs`/`prose`/`published_at`; a summarizer failure marks `memory_status: 'failed'` and the queue row `failed` without touching publication state; no compaction below the arc boundary; compaction and an `arc_summaries` insert exactly at the boundary (chapter 61 given the 50/12 constants); empty queue reports `claimed: false`. All passing.

## 4. Context policy on universe versions

- [x] 4.1 Extended `UniverseVersionInput` with optional `contextPolicy` (validated via `contextPolicySchema`, defaults to `DEFAULT_CONTEXT_POLICY`) and optional `canonBible` (free-form; a universe created without research has none). Also added a new migration (`20260819000005_universe_version_context_policy_params.sql`) giving `create_universe_with_version`/`publish_universe_version` new parameters for both — required `drop function` first since `create or replace` can't change a parameter list. `createUniverse`/`publishUniverseVersion` both gained an optional `usage: UsageRecorder` param (defaults to `nullUsageRecorder`) so canon compression can bill/log without breaking existing callers; `research/publish.ts` was updated to pass a real recorder (one-line change, keeps CLAUDE.md's "every model call writes usage_log" intact rather than silently using the null recorder). `research/publish.ts`'s `draftToUniverseVersionInput` was intentionally left NOT wired to supply `canonBible` from the draft's rules/entities/timeline/rule-pack sections — out of scope for this task group (proposal's non-goal: no change to the research pipeline's stage outputs); it now simply gets null compressed variants, which is spec-honest, not a regression.
- [x] 4.2 `compressCanonBible` in `universes.ts` — two parallel `summarizer` calls (summary + rules-only) via `callStructured`, called from both `createUniverse` and `publishUniverseVersion` before the RPC write, so both compressed columns are populated in the same insert as `entity_schema`. A `StructuredOutputError` on either call degrades that variant to null rather than failing the whole publish (a transport-level throw still propagates, matching every other role call's failure behavior elsewhere in the codebase).
- [x] 4.3 `apps/web/src/lib/engine/universes.test.ts` additions (4 new tests, all passing): a published version has both compressed variants populated when `canonBible` is supplied; omitting `canonBible` yields null variants without calling the model; omitting `contextPolicy` yields `DEFAULT_CONTEXT_POLICY`; a version's `contextPolicy` is independent of a later version's. Existing 12 tests (this file + `research/publish.test.ts`) still pass unmodified after adding the required `@/lib/env`/`@/lib/ai/usage`/`@/lib/ai/gateway` mocks this file now needs.

## 5. Context assembly extension

- [x] 5.1 Extended `ContextPolicy` with optional `retrievedChapters`/`retrievalBias`/`canonCompression` fields (`recentChapters`/`tokenBudget` stay required, matching Phase 1); `DEFAULT_CONTEXT_POLICY` gained matching defaults. No existing caller's behavior changes since all new fields are optional and unread by the render logic itself (the caller resolves canon text and retrieval upstream).
- [x] 5.2 Added `ContextRetrievedSummary` (turnNumber, optional `arcRange` for arc-level entries, summary, similarity) and `retrievedSummaries` input; renders a `## Relevant History` section positioned after Recent Events, before This Turn. Deduped by `turnNumber` against RECENT's kept chapters. Retrieved entries are lower priority than recent full-prose chapters — when the budget runs out, trimming stops at the first (pre-ranked-by-similarity) entry that doesn't fit rather than skipping ahead to a cheaper, less relevant one.
- [x] 5.3 Added `story.canonBibleText` (optional, resolved by the caller from `context_policy.canon_compression` — `assembleContext` only renders text it's handed) rendered as a `## Canon Bible` section when present. Function remains a pure, side-effect-free read of its inputs.
- [x] 5.4 `apps/web/src/lib/engine/context.test.ts` additions (8 new tests, all passing, 19 total in file): retrieved summaries appear when provided; a retrieved summary whose chapter is already in RECENT is deduped (and counted in the new `droppedRetrievedSummaries` field); deterministic/side-effect-free with the new inputs; omitting the new optional inputs reproduces exact Phase 1 output byte-for-byte; canon bible text included/omitted correctly; budget pressure drops the lowest-relevance retrieved summary first; an arc-range entry renders distinctly from a single-chapter one. All 11 pre-existing tests still pass unmodified.

## 6. Retrieval

- [x] 6.1 `apps/web/src/lib/memory/retrieval.ts`: `retrieveRelevantSummaries(args)` — embeds `queryText` via `embedder`, then calls two new security-definer RPCs (`match_chapter_summaries`, `match_arc_summaries` — added via `20260819000006_similarity_search.sql`, since PostgREST doesn't expose the `<=>` cosine-distance operator directly), both scoped to `story_id` in the query itself, merges and re-sorts by similarity, slices to K. One function, no branch keyed on universe/genre/retrieval_bias identity.
- [x] 6.2 Wired into `apps/web/src/lib/engine/turns.ts`'s `buildTurnContext`: added `universe_id`/`universe_version` to the stories select, added `resolveUniverseContext` (resolves the pinned universe version's `context_policy` — converting its snake_case DB shape to `context.ts`'s camelCase `ContextPolicy` — and compressed canon bible text per `canon_compression`), calls `retrieveRelevantSummaries` with the turn's submissions as query text and `context_policy.retrieved_chapters` as K, passes both into `assembleContext`. Unpinned stories get the Phase 1 default policy and no canon bible, unchanged from before.
- [x] 6.3 `apps/web/src/lib/memory/retrieval.test.ts` (6 tests, all passing): chapter and arc matches ranked together by similarity; result capped at k; empty story returns `[]` without erroring; every match query scoped to the given `story_id`; `k: 0` skips the DB call entirely; an arc match carries its chapter range distinctly from a single-chapter match.
- [x] 6.4 `apps/web/src/lib/memory/genre-agnosticism.test.ts` (2 tests, all passing): scans `retrieval.ts` for forbidden fixture-universe/genre/`retrieval_bias ===` patterns; asserts exactly one `.sort(` call exists, guarding against a future per-bias ranking branch. Scoped to `lib/memory/` rather than extending the Phase 2 engine-wide scanner, since that one is hardcoded to `lib/engine/`. Full suite (218 tests, `apps/web`) and typecheck both clean after this task group.

## 7. Docs (Docusaurus)

- [x] 7.1 `docs/docs/phases/phase-4-memory.md`: mirrors `phase-3-research-pipeline.md`'s structure — scope, exit criteria, what shipped/didn't (including the honest note that `research/publish.ts` doesn't yet supply `canonBible`), key design decisions, database objects, verification.
- [x] 7.2 `docs/docs/architecture/memory-and-context.md`: new page — per-chapter memory generation and its worker, retrieval mechanics and the `match_*_summaries` RPCs, arc compaction thresholds, context policy fields/defaults, retrieval-bias-via-prompt rationale, synchronous canon compression rationale. Also updated the existing (Phase-1-era, forward-looking) `architecture/context-assembly.md` in place — added a "What's implemented (Phase 4)" section rather than leaving it describing an unbuilt target, following the same pattern Phase 2 used for `schema-system.md`.
- [x] 7.3 `docs/sidebars.ts`: added `architecture/memory-and-context`, `phases/phase-4-memory`.
- [x] 7.4 `docs/docs/phases/build-order.md`: Phase 4 row linked, section marked "Status: implemented."
- [x] 7.5 `docs/docs/reference/data-model.md`: added `memory_queue`, `arc_summaries` (corrected to its real not-null/no-default shape), `chapters.embedding`/`memory_status`, `universe_versions.context_policy`/`canon_bible_summary`/`canon_bible_rules_only`; updated "What exists by phase" and the deferred-objects table.
- [x] 7.6 `npm run build` inside `docs/` — clean, no broken links.

## 8. Verification

- [x] 8.1 From `apps/web`: `npm test` (219/219 passing), `npm run typecheck` (clean), `npm run build` (compiles, all routes generated including `/api/worker/memory`) — all three pass. Also re-ran `supabase db advisors --linked` and the RLS coverage test after every migration added this phase — clean throughout (only the pre-existing, unrelated leaked-password-protection warning).
- [x] 8.2 `openspec validate phase-4-memory --strict` passes.
- [x] 8.3 `apps/web/src/lib/memory/exit-criterion.test.ts` — drives the real exported `generateChapterMemory`, `retrieveRelevantSummaries`, and `assembleContext` against a mocked gateway/database, following Phase 3's `run-research-pipeline.test.ts` pattern. A chapter-3 fact ("the Ashfall Key") is generated into a summary and a stand-in embedding; a chapter-30 query about it is embedded near that summary and far from unrelated filler; retrieval surfaces it; `assembleContext` renders it into chapter 30's context via RETRIEVED while RECENT holds only chapters 27-29 — proving the pipeline carries a chapter-3 detail forward without requiring chapters 4-29 to sit in the prompt. Real embedding geometry isn't reproducible without a live model call in this environment, so similarity is encoded directly in the fake embeddings rather than asserted as an emergent property — the test proves the pipeline's *mechanism* reaches the exit state, which is what's actually verifiable here (same reasoning Phase 3's exit-criterion test used for timing).
