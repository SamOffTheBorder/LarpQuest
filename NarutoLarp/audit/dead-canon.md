# Dead Canon — Un-Executed Retcons

Mirrors `BlackCloverLarp/audit/dead-canon.md`. When a player says "GM override" and a chapter is rewritten, **`OGFile.md` still contains both the original and replacement text** (the file is never edited once written) — this file is the pointer that marks what's void, so downstream chapters don't cite deleted material as fact.

None yet — no retcons have occurred.
